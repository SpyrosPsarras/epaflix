{
  "qualityThreshold": 80,
  "testCoverage": {
    "applicable": false,
    "reason": "Infrastructure-as-Code monorepo — no unit-test framework. Correctness is gated by CI validation + live ArgoCD reconcile state instead of coverage."
  },
  "linting": [
    {"name": "YAML parse", "tool": "python3 + PyYAML", "scope": "**/*.{yaml,yml} excl /charts/"},
    {"name": "SOPS encryption guard", "tool": ".github/hooks/check-sops-encrypted.sh", "scope": "staged kind: Secret YAML"}
  ],
  "formatting": [],
  "commitChecks": ["sops-encryption-guard"],
  "validationGates": [
    {"name": "yaml-parse", "tool": "python3 PyYAML", "blocking": true},
    {"name": "helm-pin-resolve", "tool": "helm pull", "blocking": true},
    {"name": "kustomize-build", "tool": "kustomize build (sops-free dirs)", "blocking": true}
  ],
  "deployGates": [
    "CI 'validate' job green (YAML parse, Helm pins resolve, kustomize build)",
    "Pre-commit SOPS guard passed (no plaintext kind: Secret)",
    "ArgoCD Application Synced + Healthy with zero live drift",
    "48h soak before flipping selfHeal on a newly adopted Application",
    "Per-app DB health-check scripts pass (where applicable)"
  ],
  "cicdIntegration": {
    "provider": "github-actions",
    "existingGate": ".github/workflows/ci.yml (validate — secret-free, fork-guarded)",
    "babysitterWorkflow": ".github/workflows/babysitter.yml (additive; requires ANTHROPIC_API_KEY repo secret)"
  },
  "notes": "Generated by cradle/project-install. Threshold default from BABYSITTER_QUALITY_THRESHOLD (80)."
}
