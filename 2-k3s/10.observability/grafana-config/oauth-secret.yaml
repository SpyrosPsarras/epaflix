---


apiVersion: v1
kind: Secret
metadata:
  name: grafana-oauth-secret
  namespace: observability
type: Opaque
stringData:
  client-id: "REPLACE_WITH_AUTHENTIK_CLIENT_ID"
  client-secret: "REPLACE_WITH_AUTHENTIK_CLIENT_SECRET"
