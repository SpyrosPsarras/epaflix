---
apiVersion: v1
kind: Service
metadata:
  name: mcp-hub
  namespace: mcp-hub
  labels:
    app: mcp-hub
spec:
  type: ClusterIP
  selector:
    app: mcp-hub
  ports:
    - name: http
      port: 8000
      targetPort: http
