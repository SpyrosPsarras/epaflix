apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Adopted under ArgoCD 2026-05-25 (Issue #16).
# Vendor copy of upstream v0.0.12 manifest + the IP-pool ConfigMap.
# Examples (example-custom-ip.yaml, example-nginx-lb.yaml, test-network-speed.yaml)
# are docs only — intentionally excluded from the Application.

resources:
  - cloud-controller.yaml
  - ip-pool-configmap.yaml
