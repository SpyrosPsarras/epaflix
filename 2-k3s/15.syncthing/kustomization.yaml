apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - storage.yaml
  - deployment.yaml
  - service-gui.yaml
  - service-sync.yaml
  - ingressroute-tcp.yaml

images:
  - name: syncthing/syncthing
    newTag: 1.30.0
