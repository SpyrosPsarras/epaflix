---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: newtarr-internal
  namespace: servarr
spec:
  entryPoints:
    - internal
  routes:
    - match: Host(`newtarr.epaflix.com`)
      kind: Rule
      services:
        - name: newtarr
          port: 30262
  tls:
    certResolver: cloudflare
    domains:
      - main: epaflix.com
        sans:
          - "*.epaflix.com"
