---
# Sonarr - Public Internet-facing IngressRoutes with Let's Encrypt TLS,
# fronted by Authentik forward-auth SSO (issue #176, servarr forward-auth
# rollout).
#
# Sonarr is an INTEGRATION TARGET: it is called over the public host
# (sonarr.epaflix.com) on /api by seerr, bazarr, cleanuparr and newtarr.
#
# Routing model:
#   - Route A (sonarr-https, priority 10) gates the HUMAN UI via the
#     authentik-forwardauth middleware (Authentik embedded outpost,
#     "Servarr users" group). This is the only newly-gated surface.
#   - Route C (sonarr-api, priority 20) matches PathPrefix(/api) with a
#     HIGHER priority than Route A (20 > 10) and carries NO middleware, so
#     API-key machine traffic and API deep-links keep working over the
#     public host unchanged. The Sonarr API is already API-key protected,
#     so exempting it from forward-auth does not weaken access control --
#     only the browser UI is newly gated behind SSO.
#
# Pattern mirrors newtarr/ingressroute.yaml and
# 05.traefik-deployment/ingress/traefik-dashboard-sso.yaml:
#   - UI route priority:10 + authentik-forwardauth middleware
#   - outpost route priority:15 matching PathPrefix(/outpost.goauthentik.io/)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr-https
  namespace: servarr
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`sonarr.epaflix.com`)
      kind: Rule
      priority: 10
      middlewares:
        - name: security-headers
          namespace: traefik-system
        - name: authentik-forwardauth
          namespace: traefik-system
      services:
        - name: sonarr
          port: 8989
  tls:
    certResolver: cloudflare
    domains:
      - main: epaflix.com
        sans:
          - "*.epaflix.com"
---
# Outpost route - handles auth callback on sonarr.epaflix.com
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr-outpost-https
  namespace: servarr
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`sonarr.epaflix.com`) && PathPrefix(`/outpost.goauthentik.io/`)
      kind: Rule
      priority: 15
      services:
        - name: authentik-server
          namespace: app-authentik
          port: 80
  tls:
    certResolver: cloudflare
    domains:
      - main: epaflix.com
        sans:
          - "*.epaflix.com"
---
# API route - exempts /api from forward-auth (priority 20 > 10) so API-key
# machine traffic + deep-links keep working over the public host.
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr-api
  namespace: servarr
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`sonarr.epaflix.com`) && PathPrefix(`/api`)
      kind: Rule
      priority: 20
      services:
        - name: sonarr
          port: 8989
  tls:
    certResolver: cloudflare
    domains:
      - main: epaflix.com
        sans:
          - "*.epaflix.com"
---
# HTTP to HTTPS redirect for Sonarr
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: sonarr-http
  namespace: servarr
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`sonarr.epaflix.com`)
      kind: Rule
      middlewares:
        - name: redirect-https
          namespace: traefik-system
      services:
        - name: sonarr
          port: 8989
