# What / Why

<!-- What changed and why. Reference the finding/incident that motivated it. -->

Closes #

## Test plan

<!-- Pre-merge boxes: must be run and ticked (with evidence inline) BEFORE merge.
     Post-merge boxes (soak windows, next-reboot checks): do not leave them as
     bare unchecked boxes — tick with "tracked in #NNN" and open that issue.
     Record outcomes by EDITING this description, never as a new comment. -->

- [ ] CI `validate` green after rebase onto `origin/main`

## Follow-ups

<!-- Every deferred item gets a gh issue before this PR closes. List them here. -->
